# Cloudflare Pages: set this in the Pages dashboard → Settings → Environment
# variables → "Production" so the build embeds the tunneled API URL.
#
# Local dev: leave VITE_API_BASE unset — vite.config.js proxies /api to
# http://localhost:8000 instead.
VITE_API_BASE=https://api.your-domain.example
